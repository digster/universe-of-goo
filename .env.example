# Universe of Goo has no backend and needs no environment variables today.
# This file exists as a placeholder per project convention so it is obvious
# whether any new vars are introduced in the future.
